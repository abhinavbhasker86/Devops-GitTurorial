--Execute below command to check the Branch
	git branch  --it will show all branches and "*" will be shown against your current branch where all changes will commit
	git branch  -a  --it will list all branches including hidden.
--create a new Branch
	git branch <<branchname>> --This will create a new branch with name provided
--Switch branch
	git switch <<branchname>> --This will switch the branch from crrent to branch whose name was provided
	
**Even after this if you check GitHub, this new branch will not show. And all files in local will show under same folder.
--Perform Add and Commit. 

In order to push this new branch to Github use below command:
	git push origin testbranch 
	
	**Note all files from main branch will show under newly creaed branch. If new create a new file under main branch you will still no longer in the New Branch untill merge is performed.

--Git Merge
consider you are in Main Branch and now you want to merge the changes done in other branch, then first SWITCH to main branch in Git bash and then execute below command to merge from other branch to main. (This is Vise versa in case want to merge from main to test branch)
--git merge <<branchname>>
-- then status, add commit push


--Rename Branch
git branch -m <<old_branch_name>> <<new_branch_name>> -- We can rename the branch even being in that branch

--Delete Branch
git branch -d <<Branch Name>> --YOu need to be in Main Branch to delete other Branch